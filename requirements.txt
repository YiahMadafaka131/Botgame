numpy>=1.24
Pillow>=10.0
opencv-python>=4.8   # video decoding for build-dataset (optional; lazy-imported)
torch>=2.1           # training + policy inference (optional; lazy-imported)
av>=11.0             # H.264 decode for FastCapture --fast (optional; lazy-imported)
